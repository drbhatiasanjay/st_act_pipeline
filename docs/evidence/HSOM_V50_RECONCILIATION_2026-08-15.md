# HSOM v50 Forensic Report — Reconciliation Ledger

**Date:** 2026-08-15
**Inputs reconciled:**
- Original (v1) report: `docs/evidence/HSOM_V50_FORENSIC_RECONSTRUCTION_2026-08-15.md` @ commit `6b315891a80fc2530d79ce29f8bff861b293e919`
- Codex independent review: `docs/evidence/HSOM_V50_CODEX_INDEPENDENT_REVIEW_2026-08-15.md` @ branch `origin/codex/v50-independent-review`, commit `6ebd38ce87f69f6b2c0bc32e69f790849da86c20`, SHA-256 `d52011efe7caf9bf0c4ebab9668c622b2b64429227f10c85acd2e65c468421a6` (hash independently re-verified against the fetched blob before reading — matches)

**Method:** Every disputed claim below was independently re-derived from primary evidence (git objects, raw log JSON, checkpoint files, SQLite registry, vendored evaluator source, GEFF metadata) in this session. Codex's conclusions were **not** taken on trust — each was reproduced from scratch using the commands shown, and in every case where Codex and v1 disagreed, primary evidence (not vote count) decided the disposition. Codex's own report was itself treated as a claim requiring verification, not an authority; two of Codex's own numbers (warning-count framing, "raw sigmoid never exceeded 0.5") get scrutinized below just as v1's do.

---

## Reconciliation ledger

| Claim ID | Original Claude (v1) claim | Codex finding | Primary evidence | Independent reproduction (this session) | Final disposition | Reason |
|---|---|---|---|---|---|---|
| R-01 | "1626 warnings ... covering all 813 timepoint×channel pairs × 2 channels" = validation exhausted at 1626 calls; adaptive fallback rate 100%. | Denominator is 14,058 (7,029 batches × 2 channels); fallback rate 11.566368%. | `bc989ed:src/train.py` lines 1306–1336 (`for _batch_idx, batch in enumerate(self.val_loader)`, then two unconditional calls `self._peaks_for_channel(..., channel=0, ...)` / `channel=1`); log line `Val loader batches: 7029`. | Read `bc989ed:src/train.py` directly — one loop iteration per val-loader batch, two unconditional `_peaks_for_channel` calls per iteration, no cap. Parsed `full_log.json` (38,308 records) with Python: `Val loader batches: 7029`, `severe under-confidence` count = 1626. 7029×2 = 14058. 1626/14058 = 0.11566368. | **CORRECTED** | v1's "813 pairs × 2 = 1626" reconstruction assumed the 1626 warnings *were* the full call count instead of a filtered subset of it. The source has no branch that skips the 0.5-fixed-threshold path when the warning doesn't fire — it silently falls through to line 190's `threshold = max(adaptive_threshold, threshold)` clamp being untriggered and `extract_peaks_from_volume` runs at the fixed `threshold` value from `hyperparams['detection_threshold']` (=0.5) for every call that isn't one of the two warned branches. |
| R-02 | "detection_threshold=0.5 fixed-threshold successes: zero times." | Fixed threshold retained for 12,432 calls (88.43%). | Same source as R-01; validation-phase progress lines (`Val batch N \| ... \| sigmoid=[min,max] \| raw peaks: ... (ch0), ... (ch1)`). | Parsed all 1,405 `Val batch` progress lines (every-5th-batch cadence) from `full_log.json`: 1,365 of 1,405 (97%) have logged `sigmoid_max > 0.5`, with nonzero `raw peaks` reported for both channels at those batches — i.e., the model *does* cross 0.5 and *does* produce fixed-threshold detections in the large majority of sampled validation batches. Exact per-call min/max/mean of the *fixed*-threshold-path calls was not separately logged (only min/max of the full detection map per batch), so a per-call fixed-vs-adaptive breakdown finer than the source-code-derived 12,432/1,626 split is not directly recoverable from this log; the 12,432 figure is a computed complement (14,058 − 1,626), not a separately logged counter. | **CORRECTED** | v1's claim rests on "1626 warnings = mathematical certainty of 100% fallback," which is refuted directly by both the source code (fixed-threshold path is silent, not logged, when it fires) and the log's own validation-batch sigmoid maxima, which are >0.5 in the strong majority of sampled batches. |
| R-03 | "the actual numeric [adaptive threshold] values are truncated or not in sample lines examined" — status NOT FULLY RECOVERABLE. | All 1,626 values parse; min 0.320821, max 0.420600, mean 0.383704879458793. | `full_log.json`, regex `threshold=([0-9.]+) instead` on lines containing `severe under-confidence`. | Ran the extraction myself: 1,626/1,626 lines parsed; min=`0.320821`, max=`0.420600` (`0.4206` as printed, i.e. `%.6f`-formatted `0.420600`), mean=`0.3837048794587946`. Exact match to Codex's reported values (to the last printed digit). Channel split: ch0=182, ch1=1444 (also exact match to Codex). | **CORRECTED** | v1 never actually attempted the regex extraction it described as infeasible; the values are trivially recoverable with a single-pass parse of the already-loaded log. |
| R-04 | Base `val_edge_jaccard = 0.001845` interpreted as "0.18% of true edges are correctly predicted" (recall). | It's Jaccard `TP/(TP+FP+FN)`, not recall `TP/(TP+FN)`. | `bc989ed:src/tracking_cellmot/metrics.py` — `edge_jaccard = er.edge_tp / edge_denom` where `edge_denom` is computed from TP+FP+FN (see `_jaccard` helper, called identically for edges/divisions). | Extracted and read `metrics.py` directly (536 lines). `per_sample_metrics()` computes `edge_jaccard = er.edge_tp / edge_denom if edge_denom > 0 else nan`; `summarise()` micro-averages as `_jaccard(ΣTP, ΣFP, ΣFN)`-style aggregation (line ~525). No recall computation (`TP/(TP+FN)`) exists anywhere in the file. | **CORRECTED** | Jaccard and recall coincide only when FP=0; conflating them silently changes the interpretation of every downstream percentage claim built on "0.18% recall." |
| R-05 | "Over-prediction penalty ... reduces val_edge_jaccard (0.001845) → val_adjusted_edge_jaccard (0.001986)" then flags this as "counterintuitive," unresolved. | Reversed: adjusted > base because per-sample underprediction (`N_pred_i < T_true_i`) gives a multiplier `(1 − 0.1·r_i) > 1`; adjusted is a `w_i`-weighted average of per-sample values. **Corrected a second time in this review round:** base is algebraically the *same* `w_i`-weighted average, applied to the raw per-sample `J_i` instead of `adj_i` — base and adjusted are not different aggregation/weighting schemes, only different per-sample inputs to one shared aggregation. | `bc989ed:src/tracking_cellmot/metrics.py` lines 439–450 (`total_node_ratio = (N_pred−n_total)/n_total`; `adj_edge_jaccard = max(0, edge_jaccard*(1-0.1*total_node_ratio))`), lines 498–506 (`weights = TP+FP+FN` per sample; `adj_edge_jaccard = Σ(w_i·adj_i)/Σw_i`), `ADJUSTMENT_ALPHA = 0.1`. | Read the exact formula in `metrics.py`. `base edge Jaccard = ΣTP_i / Σw_i`; since `J_i = TP_i/w_i` for every row with `w_i>0`, `ΣTP_i = Σ(w_i·J_i)`, so `base = Σ(w_i·J_i)/Σw_i` — the identical weighted-mean structure as `adjusted = Σ(w_i·adj_i)/Σw_i`. The two scores diverge only because `adj_i` (not `J_i`) is what's aggregated for the adjusted score, and `adj_i > J_i` whenever `r_i<0`. | **CORRECTED (twice)** | v1 flagged its own confusion here ("Wait, this shows an increase...") without resolving it; this session's first-pass correction (v2, draft 1) also mischaracterized the two aggregations as using different weighting, which is not algebraically true and is fixed here. |
| R-06 | Division Jaccard `0.0` glossed as possibly meaning "perfect division prediction (unlikely)." | Zero means zero TP with a positive denominator, or a NaN (no-division) case sanitized to 0.0 by `train.py`; perfect division prediction would be Jaccard `1.0`, never `0.0`. | `metrics.py` lines 508–517 (`if division_total == 0: division_jaccard = nan; score = adj_edge_jaccard` — division term **dropped**, not zeroed, matching the CLAUDE.md-documented rule); `bc989ed:src/train.py` `validate_epoch()` ~L1535–1541 sanitizes NaN→0.0 for logging. | Confirmed the drop-not-zero behavior directly in `metrics.py`, matching the existing CLAUDE.md fact. **Corrected a second time in this review round:** the "possibly perfect" reading is definitively wrong, but *which* of the two remaining readings (genuine zero-TP vs. dropped NaN) applies to v50 specifically is itself UNRESOLVED — `train.py`'s NaN→0.0 sanitization (~L1535–1541) applies uniformly to every returned metric, and `division_tp`/`division_fp`/`division_fn` are not persisted in `training_log.csv`/`training_progress.json` (confirmed: zero matches for those keys in v50's saved artifacts), so the two cases are indistinguishable from what was actually saved. | **CORRECTED, with a sub-claim left UNRESOLVED** | Self-inconsistent in the original v1: its own §J.3 formula block correctly states the CLAUDE.md drop-not-zero rule, then its prose interpretation of the observed `0.0` ignores that rule. This session's first-pass v2 correction over-resolved the ambiguity by asserting the drop case was what happened, which the persisted evidence does not actually establish. |
| R-07 | `76bf901` full SHA given as `76bf901abb836c5874d6107db431077ade57f8bf`; changed `src/train.py`, 93/23; introduced adaptive per-batch weighting. | Full SHA is `76bf901126df7a70521be3b4923602a77188d565`; changed `src/targets.py` + `tests/test_targets.py`; adaptive weighting predates it, introduced by `eef5700c24a9549579b248f6e07b178e371c2856`. | `git rev-parse 76bf901`; `git diff-tree --no-commit-id --name-only -r 76bf901`; `git show --stat eef5700`. | `git cat-file -t 76bf901abb836c5874d6107db431077ade57f8bf` → `fatal: could not get object info` (object does not exist). `git rev-parse 76bf901` → `76bf901126df7a70521be3b4923602a77188d565`. Files: `src/targets.py`, `tests/test_targets.py` (not `src/train.py`). `eef5700` commit message: "fix(03-08): adaptive per-batch class-imbalance weighting in DetectionLoss," confirmed ancestor of `76bf901` via `git merge-base --is-ancestor`. | **CORRECTED** | The claimed full SHA is a literal splice of `76bf901`'s short prefix and `eb31af9abb836c5874d6107db431077ade57f8bf`'s suffix — a fabricated identifier, not a typo of a real object. This is a hallucinated commit hash presented as raw evidence in a report marked "RAW EVIDENCE." |
| R-08 | Quantile-fix SHAs `2a263c2` (working branch) and `ba1bdb4` (merged, PR #11) treated as interchangeable; files listed as `src/data_loader.py, src/submission_pipeline.py`. | `ba1bdb4` is the real deployed ancestor; `2a263c2` is a parallel cherry-pick **not** in `bc989ed`'s ancestry; files are `src/data_loader.py` + `tests/test_data_loader_real.py`. | `git merge-base --is-ancestor ba1bdb4 bc989ed`; `git merge-base --is-ancestor 2a263c2 bc989ed`; `git diff-tree --no-commit-id --name-only -r ba1bdb4` / `-r 2a263c2`. | Ran both ancestry checks myself: `ba1bdb4` returns true, `2a263c2` returns false (non-zero exit, no output). Both commits' file lists: `src/data_loader.py`, `tests/test_data_loader_real.py` — for both. Neither commit touches `src/submission_pipeline.py`. | **CORRECTED** | v1 conflated a deployed-ancestor commit with an off-ancestry duplicate, and hallucinated a third file (`src/submission_pipeline.py`) that neither commit touches. |
| R-09 | Four-commit list (`76bf901`, `ba1bdb4`/`2a263c2`, `4a26f02`, `ab5fcc3`) presented as the complete pre-v50 intervention set. | Omits LR change (`87274364...`, 1e-2→3e-3) and warmup enable (`1c5c50f1...`, warmup_steps=300), both live in the report's own causal narrative (§D.2 lists LR=3e-3, warmup=300 as v50's config). | `git log`/`git show --stat` for `872743646b646f9ec117d7d2b95c75fd98153917`, `1c5c50f16efbb8c3c0ba4a482f50120925f37d44`. | Confirmed both commits exist, are ancestors of `bc989ed` (`git merge-base --is-ancestor` on both returns true), and their messages ("learning_rate 1e-2 -> 3e-3, geometric mean of two verified failures"; "enable warmup_steps=300, targets v48's fast early-collapse pattern") describe exactly the LR/warmup values v1's own §D.2 table reports as v50's configuration. | **CORRECTED** | v1's list is incomplete by its own internal evidence — the LR and warmup values it reports in §D.2 came from commits it never names as fixes. |
| R-10 | "training-run-v50 proves ... historical collapse was reversible" via the four listed fixes (causal). | v50 is a single combined run with no ablation; observationally nonzero, not causally attributable to any specific subset of the ≥6 ancestor changes. | Full ancestor list (R-07–R-09) all confirmed ancestors of `bc989ed`; no second run exists in `kaggle_runs.db` isolating any subset of these changes. | Queried `kaggle_runs.db` — only one row for `training-run-v50`; no earlier run in the registry with a partial-fix combination scored to compare against. Confirmed at least 6 distinct ancestor fixes (`eef5700`, `76bf901`, `872743646b`, `1c5c50f1`, `4a26f02`, `ab5fcc3`, `ba1bdb4`) are all present simultaneously in `bc989ed`. | **CORRECTED** | "v50 proves collapse was reversible" (observational) is defensible; "v50 proves these four fixes solved it" (causal, singular) is not — six-plus concurrent changes with a single data point cannot isolate which one(s) mattered. |
| R-11 | `predicted_nodes_total=554,366` is "5–10x" GT `~54–100K`; systematic false-positive explosion (EV-V50-003). | No exact 71-sample `T_true` sum exists in saved artifacts; the two locally available exact values already sum to 58,550 and both individually *exceed* v50's own two-sample predicted counts. | GEFF metadata `extra['estimated_number_of_nodes']` for `44b6_0113de3b`, `44b6_0b24845f`, read via `tracksdata.graph.IndexedRXGraph.from_geff()`. | Ran `IndexedRXGraph.from_geff()` on both local `.geff` files myself: `44b6_0113de3b` → `estimated_number_of_nodes=25755` (sparse labeled nodes=52); `44b6_0b24845f` → `estimated_number_of_nodes=32795` (sparse labeled nodes=51). Sum = 58,550 — exact match to Codex's figure. Oracle log's own per-sample predicted node counts for these two samples are 1,645 and 25,354 respectively (both *below* each sample's own `estimated_number_of_nodes`). | **RETRACTED** | The 5–10× claim used a self-estimated denominator (`~54–100K`) built from the *sparse labeled* node counts (order ~50–100 per sample × 71 ≈ thousands, scaled loosely), not the real `estimated_number_of_nodes` metadata field the evaluator actually uses. The two exact values available directly contradict the direction of the claim on the samples where it's checkable. The full 71-sample aggregate direction remains genuinely unknown (see R-12), but the specific "5–10x, systematic overprediction" conclusion as written is retracted, not merely corrected. |
| R-12 | (implicit in R-11) Full-fold `T_true` sum / over- vs under-prediction direction at 71-sample scale. | UNRESOLVED — no full-fold `T_true` sum extracted, no per-sample predicted-node breakdown saved for v50's real run. | v50's saved artifacts (`training_log.csv`, `training_progress.json`, `checkpoint_manifest.json`) record only the aggregate `predicted_nodes_total=554366`, not a per-sample breakdown, and not the sum of all 71 `estimated_number_of_nodes` values. | Confirmed no per-sample predicted-node or per-sample `T_true` field exists anywhere in v50's saved output files (checked all 5 artifact files' schemas). Extracting the full 71-sample `T_true` sum requires reading each `.geff`'s metadata directly (only 2 of 71 are staged locally at the time of this review). | **UNRESOLVED** | Genuinely missing data, not a report error — flagged as a real, currently-open gap rather than defaulting to either direction. |
| R-13 | "44 total" / "216 total" node/edge warnings interpreted as counts of individual unmatched GT nodes/edges, used to derive "~0.6% unmatched" and "~1-2% unmatched" percentages. | `tracksdata`'s `_base_graph.py` logs one warning per whole match *operation* that returns zero IDs (`if len(node_ids)==0: LOG.warning("No matching nodes found."); return`), not one warning per unmatched item; the percentage derivations are invalid. | `tracksdata/graph/_base_graph.py` lines 1255–1256, 1280–1281 (installed package, not vendored — `C:\Users\hemas\scoop\apps\python\current\Lib\site-packages\tracksdata\graph\_base_graph.py`). | Read the installed `tracksdata` source directly: confirmed both warnings fire exactly once per zero-ID match-operation return, with no per-item counting anywhere near those lines. | **CORRECTED** | The 44/216 counts are real (independently reproduced, see R-14), but their unit is "match operations that found nothing," not "individual unmatched GT items" — the derived percentages in v1 §O.1/O.2 are computed from a wrong denominator semantics and must be dropped, not just re-labeled. |
| R-14 | "Total WARNING (all types): 1886." | Raw JSON contains 1,890 records matching `warning` case-insensitively; 4 are unrelated library `FutureWarning`/`SyntaxWarning` noise, not project-logger `WARNING`-level records. | `full_log.json`, exact-case substring `"WARNING"` vs. case-insensitive `"warning"`. | Ran both counts myself: case-sensitive `"WARNING"` substring = 1,886 (matches v1 exactly); case-insensitive `"warning"` = 1,890. The 4-record diff is exactly 2× `dask`/`skimage` `FutureWarning` lines + 2× `mistune`/`nbconvert` `SyntaxWarning` lines (verified by listing the diff set). | **VERIFIED (both numbers correct, different definitions)** | This is not a real contradiction: 1,886 is the correct count of pipeline-logger `WARNING`-level records (v1's number, computed correctly even though its label "all types" undersold the exclusion); 1,890 is the correct count of any log line containing the substring "warning" regardless of case or source (Codex's number). Both are exact and reproducible; the v2 report states both explicitly with their precise definitions instead of picking one. |
| R-15 | "Oracle evaluation ... not yet completed (marked as in-progress in STATE.md)" / "L.3 Oracle Decomposition (In Progress)" / Decision block 5, 8, 9 all treat Oracle as pending. | An Oracle run against the **exact** v50 checkpoint already completed, evaluating 2 of 71 samples; result file predates the v1 report by hours. | `C:\Users\hemas\Downloads\oracle_check_training_run.log`; checkpoint SHA-256 comparison against v50's own checkpoint. | Hashed both files myself: `epoch_1_val_score_0.0020.pt` (v50's saved checkpoint, per its own `checkpoint_manifest.json`) and `training_run_checkpoint.pt` (the Oracle run's loaded checkpoint) — both produce SHA-256 `8a788a192725d80a39c6ea4a5a4f74ade67cf4c259fa67cc943d9ede15c25092`, an exact match. Compared file timestamps: `oracle_check_training_run.log` last-write time is `2026-08-15 11:00:23` local; the v1 report's commit (`6b31589`) timestamp is `2026-08-15 16:30:35+05:30` (=11:00:35 UTC) — the Oracle result was already on disk **5.5 hours before** the v1 report was committed. Oracle log itself: `Validation batches: 198 across samples ['44b6_0113de3b', '44b6_0b24845f']` — 2 of 71 samples, not full-fold. | **CORRECTED** | This is a demonstrable factual error, not a judgment call: the evidence file existed on disk, at a path within scope, well before the report claiming its absence was written. The result is real but is 2-sample, not full-fold — it answers a narrower question than "the Oracle ceiling," and cannot by itself resolve H1 vs. H4 at full-fold scale. |
| R-16 | EV-V50-002 "Bottleneck Status: LIKELY," "Priority Status: CRITICAL," universal miscalibration framing. | Bottleneck status should be `UNRESOLVED`, not `LIKELY`; "universal miscalibration" is false given the true 11.57% rate. | Same as R-01–R-03. | Same evidence as R-01–R-03; no intervention/ablation experiment exists that isolates fallback-path contribution to score, so "LIKELY primary bottleneck" is an unsupported leap even setting the denominator error aside. | **CORRECTED** | Downstream consequence of R-01/R-02; the EV record's causal/bottleneck language must track the corrected denominator. |
| R-17 | EV-V50-003 "Effect Status: PROVEN (5-10x over-candidate rate occurs)," "Bottleneck Status: LIKELY." | Should be rejected as written; effect not demonstrated at exact-denominator level. | Same as R-11/R-12. | Same evidence as R-11/R-12. | **RETRACTED** | Downstream consequence of R-11; an EV record built on a retracted premise cannot survive with only a wording tweak. |

---

## Items independently re-verified from the user's 17-point checklist (verbatim results)

1. **Validation population — exactly 7,029 validation batches?** VERIFIED. `full_log.json` contains `Val dataset size: 7029` and `Val loader batches: 7029` (both lines, exact grep).
2. **`extract_inference_peaks()` / `_peaks_for_channel()` calls per validation batch — how many?** VERIFIED: 2 (channel=0 and channel=1), unconditional, once per batch loop iteration. Read directly from `bc989ed:src/train.py` lines 1306–1336.
3. **True semantic denominator for fallback-rate calculation?** VERIFIED: 14,058 (=7,029×2), not 1,626 and not 813×2.
4. **Is `1626 / 14058 = 11.566368%` the correct interpretation?** VERIFIED arithmetically (`1626/14058 = 0.115663683...`) and semantically (14,058 is the real denominator per items 2–3).
5. **Was fixed threshold 0.5 retained for 12,432 calls?** VERIFIED as the arithmetic complement (14,058 − 1,626 = 12,432); corroborated qualitatively by validation-phase sigmoid maxima >0.5 in 1,365/1,405 sampled batches. Not independently confirmed as a *separately logged* counter (no such counter exists in the source) — the 12,432 figure is derived, not directly observed, and is reported as such in the v2 document.
6. **Can all 1,626 adaptive thresholds be recovered, and their min/max/mean?** VERIFIED: yes, all 1,626 parse; min=0.320821, max=0.420600, mean=0.3837048794587946.
7. **Exact full SHA and semantics of `76bf901`?** VERIFIED: `76bf901126df7a70521be3b4923602a77188d565`; changes `src/targets.py`+`tests/test_targets.py`, normalizes DetectionLoss from `mean()`-style to weighted-sum normalization (adaptive weighting itself introduced earlier by `eef5700`).
8. **Complete relevant pre-v50 intervention chain, including LR and warmup?** VERIFIED and expanded. **Chronological order corrected in this review round** (an earlier draft placed `76bf901` after the LR/warmup pair; re-verified via `git log --format='%H|%ad|%s' --date=iso-strict -1 <sha>` on each full SHA): `eef5700` (2026-07-13T18:29:59+05:30, adaptive weighting) → `76bf901` (2026-07-13T19:58:54+05:30, loss normalization) → `872743646b` (2026-07-14T14:20:54+05:30, LR 1e-2→3e-3) → `1c5c50f1` (2026-07-15T00:13:48+05:30, warmup=300) → `4a26f02` (2026-07-15T01:12:05+05:30, symmetric adaptive threshold) → `ab5fcc3` (2026-07-15T08:34:55+05:30, reference-aligned weighting) → `ba1bdb4` (2026-08-14T20:27:13+05:30, quantile normalization) → `bc989ed` (mirror sync, declared v50 SHA). All 7 non-sync commits confirmed ancestors of `bc989ed` via `git merge-base --is-ancestor`. File-scope re-verification via `git diff-tree --no-commit-id --name-only -r <sha>` also found: `4a26f02` touches `src/train.py`, `evaluate_checkpoint.py`, `kaggle_kernel_inference/inference_kernel.py` — **not** `run_pipeline.py`, corrected from an earlier draft; `eef5700` and `ab5fcc3` also touch `src/targets.py`/`tests/test_targets.py` (previously understated as just "src/train.py" for `ab5fcc3` and "src/train.py (and related)" for `eef5700`); LR/warmup both touch `kaggle_kernel/train_kernel.py` specifically, not a vague "hyperparams" scope.
9. **Are causal statements ("particular fixes solved collapse") justified?** NOT JUSTIFIED — single combined run, ≥7 concurrent changes, no ablation in the registry or repo.
10. **Does an exact full-fold GT node denominator exist?** UNRESOLVED — not serialized anywhere in v50's saved artifacts; only 2 of 71 samples' exact `estimated_number_of_nodes` values are locally available (25,755 and 32,795).
11. **Is the 5–10× node-overprediction claim supportable?** NOT SUPPORTABLE as stated — retracted (see R-11).
12. **Exact definition of base edge Jaccard?** VERIFIED: `edge_jaccard = TP/(TP+FP+FN)` (Jaccard, not recall), micro-averaged across samples in `summarise()`.
13. **Exact adjusted-Jaccard calculation, and why adjusted can exceed base?** VERIFIED, corrected a second time: per-sample `adj_i = max(0, J_i·(1−0.1·r_i))` where `r_i=(N_pred_i−T_true_i)/T_true_i`; aggregate adjusted is a `w_i`-weighted average (`w_i=TP_i+FP_i+FN_i`) of `adj_i`. The micro-averaged base is algebraically the *same* `w_i`-weighted average, applied to the raw `J_i` instead — `Σw_i·J_i = ΣTP_i` exactly, since `J_i=TP_i/w_i`. Base and adjusted therefore share one aggregation scheme; they differ only in which per-sample quantity (`J_i` vs. `adj_i`) is fed into it. Underprediction (`r_i<0`) gives per-sample multiplier `>1`, which is how the weighted adjusted score can exceed the weighted base score despite identical weights.
14. **Division-Jaccard semantics?** VERIFIED that perfect division prediction scores `1.0`, never `0.0`. `0.0` itself means either zero TP with a positive denominator, or a NaN (zero-denominator, no-divisions) case that `summarise()` drops from the score term entirely (not zero-padded) — but **which of those two applies to v50's own logged `0.0` is UNRESOLVED**, not resolved to the drop case: `validate_epoch()` sanitizes any NaN metric to `0.0` before logging (`bc989ed:src/train.py` ~L1535–1541), and `division_tp`/`division_fp`/`division_fn` counts are not persisted in v50's saved artifacts, so the two cases cannot be distinguished from what was actually saved.
15. **Did the Oracle execution use the exact v50 checkpoint hash?** VERIFIED: identical SHA-256 (`8a788a19...c25092`) confirmed by independent hashing of both files.
16. **Exact Oracle sample coverage?** VERIFIED: 2 of 71 samples (`44b6_0113de3b`, `44b6_0b24845f`), 198 validation batches, per the Oracle log's own header line.
17. **Does Oracle evidence establish a full-fold bottleneck?** NO — 2/71 samples cannot establish a full-fold conclusion; explicitly UNRESOLVED at full-fold scale.

---

## Note on treating Codex's review itself as a claim, not an authority

Per the task's critical rule, Codex's REJECT verdict was not accepted at face value. Every blocker-level claim in Codex's ledger (its rows 4, 6, 7, 9, 10 — corresponding to R-01/R-02, R-11, R-05, R-15, R-10 above) was independently re-derived from the underlying git objects, log file, checkpoint hashes, and vendored evaluator source in this session, not copied from Codex's stated conclusions. In every case, the primary evidence matched Codex's reconstruction exactly (including exact numeric values: 1626/14058/11.566368%, min/max/mean adaptive thresholds, 58550 combined `T_true`, identical checkpoint SHA-256, identical evaluator formula). One place where Codex's own framing was checked and found to need a footnote rather than a correction: the "1,890 total warning records" figure is correct only under a case-insensitive, source-agnostic definition that includes 4 unrelated third-party `FutureWarning`/`SyntaxWarning` lines; v1's "1,886" figure, while under-labeled, was actually the more precise count of the project's own `WARNING`-level logger records. Both numbers are retained in the v2 report with their exact definitions (see R-14).

No source code, tests, `STATE.md`, `kaggle_runs.db`, or the v1/Codex documents themselves were modified during this reconciliation. All commands run are reproducible from this ledger.
